<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@drawable/bg_layout_white_radius">

        <TextView
            android:id="@+id/tv_title_logout"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Logout"
            android:textColor="@android:color/black"
            android:textSize="20sp"
            android:textStyle="bold"
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            android:layout_marginLeft="20dp"
            android:layout_marginTop="20dp" />

        <TextView
            android:id="@+id/tv_logout"
            android:layout_width="300dp"
            android:layout_height="wrap_content"
            android:text="Are you sure? Logging out will remove all data from  this device."
            android:textColor="@android:color/black"
            android:textSize="15sp"
            app:layout_constraintTop_toBottomOf="@+id/tv_title_logout"
            app:layout_constraintLeft_toLeftOf="@+id/tv_title_logout"/>

        <Button
            android:id="@+id/btn_ok_logout"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="OK"
            android:background="#fff"
            app:layout_constraintTop_toBottomOf="@+id/tv_logout"
            app:layout_constraintRight_toRightOf="@+id/tv_logout"
            android:layout_marginTop="20dp"/>

        <Button
            android:id="@+id/btn_cancel_logout"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Cancel"
            android:background="#fff"
            app:layout_constraintTop_toTopOf="@+id/btn_ok_logout"
            app:layout_constraintRight_toLeftOf="@+id/btn_ok_logout"
            android:layout_marginEnd="10dp"
            app:layout_constraintBottom_toBottomOf="parent"
            android:layout_marginBottom="20dp"
            />
    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>